import 'package:flutter/material.dart';

class CountryDetailWeb extends StatelessWidget {
  const CountryDetailWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Country Detail Web'),
    );
  }
}
