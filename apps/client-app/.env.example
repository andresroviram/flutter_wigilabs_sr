# ──────────────────────────────────────────────────────────────────────────
#  Template de variables de entorno
#  Copia este archivo como .env.dev, .env.qa o .env.prod y rellena
#  los valores reales. NUNCA commitees archivos con secretos reales.
#
#  Uso local:
#    cp .env.example .env.dev  → editar con valores de desarrollo
#    cp .env.example .env.qa   → editar con valores de QA
#    cp .env.example .env.prod → editar con valores de producción
#
#  Después de editar cualquier .env.*, regenera el código (desde la raíz):
#    melos run build:all
#
#  Para correr la app con un flavor (desde apps/client-app):
#    flutter run --dart-define=FLAVOR=dev
#    flutter run --dart-define=FLAVOR=qa
#    flutter run --dart-define=FLAVOR=prod
# ──────────────────────────────────────────────────────────────────────────

# URL base de la API REST
BASE_URL=https://restcountries.com/v3.1

# Clave de API (vacío si no se necesita)
API_KEY=